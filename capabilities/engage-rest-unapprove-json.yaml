naftiko: 1.0.0-alpha2
info:
  label: Marketo Engage Rest API — Unapprove.json
  description: 'Marketo Engage Rest API — Unapprove.json. 1 operations. Lead operation: Marketo Unapprove Snippet. Self-contained
    Naftiko capability covering one Marketo business surface.'
  tags:
  - Marketo
  - Unapprove.json
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MARKETO_API_KEY: MARKETO_API_KEY
capability:
  consumes:
  - type: http
    namespace: engage-rest-unapprove-json
    baseUri: https://localhost:8080
    description: Marketo Engage Rest API — Unapprove.json business capability. Self-contained, no shared references.
    resources:
    - name: rest-asset-v1-snippet-id-unapprove.json
      path: /rest/asset/v1/snippet/{id}/unapprove.json
      operations:
      - name: unapprovesnippetusingpost
        method: POST
        description: Marketo Unapprove Snippet
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: id
          required: true
  exposes:
  - type: rest
    namespace: engage-rest-unapprove-json-rest
    port: 8080
    description: REST adapter for Marketo Engage Rest API — Unapprove.json. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/asset/v1/snippet/{id}/unapprove-json
      name: rest-asset-v1-snippet-id-unapprove-json
      description: REST surface for rest-asset-v1-snippet-id-unapprove.json.
      operations:
      - method: POST
        name: unapprovesnippetusingpost
        description: Marketo Unapprove Snippet
        call: engage-rest-unapprove-json.unapprovesnippetusingpost
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: engage-rest-unapprove-json-mcp
    port: 9090
    transport: http
    description: MCP adapter for Marketo Engage Rest API — Unapprove.json. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: marketo-unapprove-snippet
      description: Marketo Unapprove Snippet
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: engage-rest-unapprove-json.unapprovesnippetusingpost
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
