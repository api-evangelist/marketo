naftiko: 1.0.0-alpha2
info:
  label: Marketo Engage Rest API — Approve
  description: 'Marketo Engage Rest API — Approve. 6 operations. Lead operation: Marketo Approve Email Draft. Self-contained
    Naftiko capability covering one Marketo business surface.'
  tags:
  - Marketo
  - Approve
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MARKETO_API_KEY: MARKETO_API_KEY
capability:
  consumes:
  - type: http
    namespace: engage-rest-approve
    baseUri: https://localhost:8080
    description: Marketo Engage Rest API — Approve business capability. Self-contained, no shared references.
    resources:
    - name: rest-asset-v1-email-id-approveDraft.json
      path: /rest/asset/v1/email/{id}/approveDraft.json
      operations:
      - name: approvedraftusingpost
        method: POST
        description: Marketo Approve Email Draft
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: id
          required: true
    - name: rest-asset-v1-emailTemplate-id-approveDraft.json
      path: /rest/asset/v1/emailTemplate/{id}/approveDraft.json
      operations:
      - name: approvedraftusingpost1
        method: POST
        description: Marketo Approve Email Template Draft
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: id
          required: true
    - name: rest-asset-v1-form-id-approveDraft.json
      path: /rest/asset/v1/form/{id}/approveDraft.json
      operations:
      - name: approvefromusingpost
        method: POST
        description: Marketo Approve Form Draft
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: id
          required: true
    - name: rest-asset-v1-landingPage-id-approveDraft.json
      path: /rest/asset/v1/landingPage/{id}/approveDraft.json
      operations:
      - name: approvelandingpageusingpost
        method: POST
        description: Marketo Approve Landing Page Draft
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: id
          required: true
    - name: rest-asset-v1-landingPageTemplate-id-approveDraft.json
      path: /rest/asset/v1/landingPageTemplate/{id}/approveDraft.json
      operations:
      - name: approvelandingpagetemplateusingpost
        method: POST
        description: Marketo Approve Landing Page Template Draft
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: id
          required: true
    - name: rest-asset-v1-snippet-id-approveDraft.json
      path: /rest/asset/v1/snippet/{id}/approveDraft.json
      operations:
      - name: approvesnippetusingpost
        method: POST
        description: Marketo Approve Snippet Draft
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: id
          required: true
  exposes:
  - type: rest
    namespace: engage-rest-approve-rest
    port: 8080
    description: REST adapter for Marketo Engage Rest API — Approve. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/asset/v1/email/{id}/approvedraft-json
      name: rest-asset-v1-email-id-approvedraft-json
      description: REST surface for rest-asset-v1-email-id-approveDraft.json.
      operations:
      - method: POST
        name: approvedraftusingpost
        description: Marketo Approve Email Draft
        call: engage-rest-approve.approvedraftusingpost
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/asset/v1/emailtemplate/{id}/approvedraft-json
      name: rest-asset-v1-emailtemplate-id-approvedraft-json
      description: REST surface for rest-asset-v1-emailTemplate-id-approveDraft.json.
      operations:
      - method: POST
        name: approvedraftusingpost1
        description: Marketo Approve Email Template Draft
        call: engage-rest-approve.approvedraftusingpost1
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/asset/v1/form/{id}/approvedraft-json
      name: rest-asset-v1-form-id-approvedraft-json
      description: REST surface for rest-asset-v1-form-id-approveDraft.json.
      operations:
      - method: POST
        name: approvefromusingpost
        description: Marketo Approve Form Draft
        call: engage-rest-approve.approvefromusingpost
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/asset/v1/landingpage/{id}/approvedraft-json
      name: rest-asset-v1-landingpage-id-approvedraft-json
      description: REST surface for rest-asset-v1-landingPage-id-approveDraft.json.
      operations:
      - method: POST
        name: approvelandingpageusingpost
        description: Marketo Approve Landing Page Draft
        call: engage-rest-approve.approvelandingpageusingpost
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/asset/v1/landingpagetemplate/{id}/approvedraft-json
      name: rest-asset-v1-landingpagetemplate-id-approvedraft-json
      description: REST surface for rest-asset-v1-landingPageTemplate-id-approveDraft.json.
      operations:
      - method: POST
        name: approvelandingpagetemplateusingpost
        description: Marketo Approve Landing Page Template Draft
        call: engage-rest-approve.approvelandingpagetemplateusingpost
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/asset/v1/snippet/{id}/approvedraft-json
      name: rest-asset-v1-snippet-id-approvedraft-json
      description: REST surface for rest-asset-v1-snippet-id-approveDraft.json.
      operations:
      - method: POST
        name: approvesnippetusingpost
        description: Marketo Approve Snippet Draft
        call: engage-rest-approve.approvesnippetusingpost
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: engage-rest-approve-mcp
    port: 9090
    transport: http
    description: MCP adapter for Marketo Engage Rest API — Approve. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: marketo-approve-email-draft
      description: Marketo Approve Email Draft
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: engage-rest-approve.approvedraftusingpost
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: marketo-approve-email-template-draft
      description: Marketo Approve Email Template Draft
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: engage-rest-approve.approvedraftusingpost1
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: marketo-approve-form-draft
      description: Marketo Approve Form Draft
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: engage-rest-approve.approvefromusingpost
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: marketo-approve-landing-page-draft
      description: Marketo Approve Landing Page Draft
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: engage-rest-approve.approvelandingpageusingpost
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: marketo-approve-landing-page-template
      description: Marketo Approve Landing Page Template Draft
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: engage-rest-approve.approvelandingpagetemplateusingpost
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: marketo-approve-snippet-draft
      description: Marketo Approve Snippet Draft
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: engage-rest-approve.approvesnippetusingpost
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
